{
  "name": "com.ksmartai.app",
  "version": "1.0.0",
  "main": "index.html",
  "scripts": {
    "build": "mkdir -p www && cp index.html www/",
    "add-android": "npx cap add android",
    "sync": "npx cap sync"
  },
  "dependencies": {
    "@capacitor/android": "^5.0.0",
    "@capacitor/core": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  }
}
